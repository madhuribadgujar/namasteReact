#namaste React
#npm run start

# Redux Toolkit
  - Install @reduxjs/toolkit and react-redux
  - build our store
  - connect our store to our app
  - create slice(cartSlice)
  - dispatch action
  - read data using selector
#testing
  - Unit Testing
  - Integration Testing
  - End-to-End Testing

  #Setting up Testing
  - Install Jest and React Testing Library
  - Configure Jest
  - Install Babel depencancies
  - Configure Babel
  - configure parcel config file to disable default bable transpition
- npm run test
- npx jest --init #initalize jest create config file for jest
- Jest configuration
- Install jsdom library
- install @babel/preset-react to make JSX work in testcases
- need to include @babel/preset-react in bable config file
- ['@babel/preset-react', { runtime: 'automatic' }]
